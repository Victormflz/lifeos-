PORT=3001
MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster.mongodb.net/lifeos
JWT_SECRET=cambia_esto_por_una_cadena_larga_y_aleatoria
NODE_ENV=development
# En producción (Railway): reemplaza con la URL real de Vercel tras el primer deploy
ALLOWED_ORIGIN=https://tu-app.vercel.app

# Solo para el script create-admin — nunca dejar valores reales aquí
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=
